/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Remove or comment out the images configuration if not needed
    // images: {
    //   domains: ['your-mongodb-domain.com'],
    // },
  }
  
  module.exports = nextConfig